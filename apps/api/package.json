{
  "name": "@closetsearch/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json && mkdir -p dist/db && cp -R src/db/schema dist/db/schema && cp -R dist/apps/api/src/. dist/",
    "start": "node dist/server.js",
    "lint": "eslint src --ext .ts && eslint vitest.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "db:migrate": "tsx src/db/migrations.ts",
    "db:seed": "tsx src/db/seed.ts"
  },
  "dependencies": {
    "@closetsearch/providers": "workspace:*",
    "@closetsearch/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0"
  }
}
