# Beta Feature Request

- Problem:
- Requested improvement:
- Why it matters:
- Workaround today:
- Urgency: `launch-blocking` | `important for beta` | `later`
- Related feature area: auth/session | feed/search | providers/data quality | saved features | personalization | analytics | watchlists | mobile/responsive | docs/setup | privacy/trust copy | performance | other

## Notes

- Keep this focused on a user problem, not only an implementation idea.
- If the request is outside beta scope, capture it for post-beta planning instead of expanding the active milestone.
