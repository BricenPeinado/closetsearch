# Shared Package

This package contains shared domain types and small utilities used across ClosetSearch apps and packages.

Current status: starter TypeScript interfaces only.

Keep this package focused on stable domain concepts. Avoid provider-specific fields and app-specific UI concerns here.
